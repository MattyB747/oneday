{
  "name": "tempo",
  "version": "0.1.0",
  "private": true,
  "description": "AI destination optimiser for Cape Town.",
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server/index.js",
    "dev": "NODE_ENV=development node server/index.js",
    "check": "node --check server/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
